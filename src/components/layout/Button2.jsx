import Modal from './Modal';

// Função para criação de botão que aparece dentro dos cards, e que direciona para o modal

function Button() {
    return (
        <section>
            <Modal />
        </section>     
      
    );
  }
  
  export default Button;